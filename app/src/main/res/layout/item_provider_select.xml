<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?listPreferredItemHeight"
    android:clickable="true"
    android:focusable="true"
    android:background="@android:color/white"
    android:layout_marginBottom="1dp"
    android:padding="@dimen/item_padding"
    android:foreground="?selectableItemBackground">

    <TextView
        android:id="@+id/item_select_provider_provider"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimaryText"
        android:textSize="16dp"
        android:text="12345" />

    <TextView
        android:id="@+id/item_select_provider_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/item_select_provider_provider"
        android:layout_marginLeft="10dp"
        android:textColor="@color/colorPrimaryText"
        android:textSize="16dp"
        android:text="Matriosca perestroica inturstroica"/>

    <TextView
        android:id="@+id/item_select_provider_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/item_select_provider_name"
        android:layout_alignParentRight="true"
        android:layout_marginTop="5dp"
        android:textColor="@color/colorAccent"
        android:text="12/10/2019" />

</RelativeLayout>